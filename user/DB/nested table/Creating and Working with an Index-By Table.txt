DECLARE
    -- Declare an index-by table
    TYPE emp_index_by_type IS TABLE OF VARCHAR2(50) INDEX BY PLS_INTEGER;
    emp_names emp_index_by_type;
BEGIN
    -- Add employee names
    emp_names(1) := 'Aarav';
    emp_names(2) := 'Vivaan';
    emp_names(3) := 'Sanchit Rawool';
    emp_names(4) := 'Anaya';
    emp_names(5) := 'Diya';

    -- Loop through the index-by table and display names
    FOR i IN 1 .. 5 LOOP
        DBMS_OUTPUT.PUT_LINE('Employee Name: ' || emp_names(i));
    END LOOP;
END;
/
