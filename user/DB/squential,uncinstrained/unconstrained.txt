DECLARE
   v_counter NUMBER := 1;
BEGIN
   LOOP
      DBMS_OUTPUT.PUT_LINE('Counter: ' || v_counter);

      v_counter := v_counter + 1;

      IF v_counter > 5 THEN
         EXIT; -- Exit loop when counter is greater than 5
      END IF;
   END LOOP;
END;
