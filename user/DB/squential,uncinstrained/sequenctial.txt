DECLARE
   v_message VARCHAR2(50);
   v_sum     NUMBER;
BEGIN
   -- Sequential statements
   v_message := 'Hello, SYCS student!';
   v_sum := 5 + 10;

   DBMS_OUTPUT.PUT_LINE(v_message);
   DBMS_OUTPUT.PUT_LINE('The sum is: ' || v_sum);
END;
