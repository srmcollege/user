DECLARE
   v_num1 NUMBER := &num1; -- Takes input from the user
   v_num2 NUMBER := &num2; -- Another input
   v_sum  NUMBER;
BEGIN
   v_sum := v_num1 + v_num2;
   DBMS_OUTPUT.PUT_LINE('The sum is: ' || v_sum);
END;
