CREATE SEQUENCE my_sequence 
   START WITH 1 
   INCREMENT BY 1 
   MINVALUE 1 
   MAXVALUE 100 
   CYCLE;

CREATE TABLE my_table (id NUMBER PRIMARY KEY, name VARCHAR2(50));

INSERT INTO my_table (id, name) VALUES (my_sequence.NEXTVAL, 'Sanchit Rawool');
INSERT INTO my_table (id, name) VALUES (my_sequence.NEXTVAL, 'Another Name');

-- Display the inserted data
BEGIN
   FOR rec IN (SELECT * FROM my_table) LOOP
      DBMS_OUTPUT.PUT_LINE('ID: ' || rec.id || ' Name: ' || rec.name);
   END LOOP;
END;
/

COMMIT;
