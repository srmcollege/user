DECLARE
   v_num NUMBER := 10;
BEGIN
   IF v_num > 10 THEN
      DBMS_OUTPUT.PUT_LINE('The number is greater than 10');
   ELSE
      DBMS_OUTPUT.PUT_LINE('The number is 10 or less');
   END IF;
      DBMS_OUTPUT.PUT_LINE('Value of v_num is:'||v_num);
END;